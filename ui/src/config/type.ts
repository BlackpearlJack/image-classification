export type Player = {
  /**
   * The player's name.
   */
  name: string;
  /**
   * The player's image URL.
   */
  image: string;
  /**
   * The player's data identifier.
   */
  dataPlayer: string;
  /**
   * The className of player card to add styling
   */
  className?: string;
};
