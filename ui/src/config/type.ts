export type Player = {
  /**
   * The player's name.
   */
  name: string;
  /**
   * The player's image URL.
   */
  image: string;
  /**
   * The player's data identifier.
   */
  dataPlayer: string;
};